#include "jacobi.h"
#include <iostream>
#include <cmath>
#include <armadillo>

using namespace std;
using namespace arma;

void jacobi_method(mat **A, mat **R, int n)
{
    /* Jacobi's algorithm
     * A : matrix
     * R : eigenvector matrix
     */
    return;
}

void maxoffdiag(mat **A, int *k, int *l, int n)
{
    /* Finds the maximum off-diagonal matrix element
     * A : matrix
     * k : index
     * l : index
     */

    return;
}
